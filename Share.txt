redirect_url=$(curl -s -i -X POST 'https://api-cumulus-dev.ubsdev.net/cumu-distribution-hub' \
  -H 'Authorization: Bearer <your_token>' \
  -H 'Content-Type: application/json' \
  -d '{
        "fileName": "govt_agency_regl_lamr.cax",
        "businessDate": "2025-04-27",
        "vendor": "bloomberg",
        "vendorAccount": "CumulusbuysidecredDL266332"
      }' | grep -i '^Location:' | cut -d' ' -f2 | tr -d '\r')

# Download the file
curl -L "$redirect_url" -o downloaded_file.cax
