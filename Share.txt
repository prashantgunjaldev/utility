config.setJdbcUrl("jdbc:postgresql://localhost:5432/mydb");
        config.setUsername("user");
        config.setPassword("password");
        config.setDriverClassName("org.postgresql.Driver");

        // Pool settings
        config.setMaximumPoolSize(10); // Max connections in pool
        config.setMinimumIdle(5);      // Min idle connections
        config.setIdleTimeout(30000);  // 30 sec timeout for idle connections
        config.setMaxLifetime(1800000); // 30 min max connection lifetime
        config.setConnectionTimeout(30000); // 30 sec timeout to get a connection
